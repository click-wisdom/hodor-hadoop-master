require 'spec_helper'

describe Hodor::Master do
  it 'has a version number' do
    expect(Hodor::Master::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
